version: '3.8'
services:
  bark-server:
    image: finab/bark-server
    container_name: bark-server
    restart: unless-stopped
    volumes:
      - /mnt/SDD128G/bark/data:/data
    ports:
      - "18001:8080"
