# Preserve the externally visible non-standard HTTPS port for ESPHome.
# ESPHome's Tornado WebSocket handler requires Origin and Host to match.
map $host $npm_upstream_host {
    default                       $host;
    esphome.xuchengcat.cn         $http_host;
}
